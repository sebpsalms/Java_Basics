public class dataTypes {

  public static void main(String args[])
		{
      int number2 =3; // variable declaration with variable type assigning
      byte by =127; // only upto 127
      
      short sh =123;
      long lo = 1234l; // need to give "l" at the end to represent long
      float fl =2.5f; // need to give "f" at the end to represent float
      double db = 2.4362572345;
      char ch = 'k'; // need to give in single quotes
      boolean b = true;

			System.out.println( number2 + "," + by + "," + sh +"," + lo + "," + fl +"," + db + "," +  ch );
		}
  
}
